export enum Status {
    ACTIVE = 'ACTIVE',
    INACTIVE = 'INACTIVE',
    REMOVED = 'REMOVED',
}

export enum TypeDiscount {
    PERCENT = 'PERCENT',
    VALUE = 'VALUE',
}

export enum Currency {
    USD = 'USD',
    VND = 'VND',
}